name: Optimized CI with Caching

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Required identification step
      - name: 23f2005552@ds.study.iitm.ac.in
        run: echo "Workflow for Kailash sir"

      # Step 1: Cache Setup
      - name: Cache dependencies
        id: cache-step
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: cache-047c730

      # Step 2: The specific step that was missing
      - name: prime-cache-047c730
        run: echo "Cache hit status is ${{ steps.cache-step.outputs.cache-hit }}"
